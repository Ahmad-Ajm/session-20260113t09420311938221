name: CI Validation

on:
  workflow_dispatch:
    inputs:
      featureKey:
        description: 'Feature key'
        required: true
        type: string
      attempt:
        description: 'Attempt number'
        required: true
        type: string

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
      - name: Run validation
        run: |
          echo "CI Validation for ${{ github.event.inputs.featureKey }}"
          echo "All good!"